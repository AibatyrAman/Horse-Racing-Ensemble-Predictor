# ─── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# ─── Sanal ortamlar ───────────────────────────────────────
.venv/
venv/
env/
# yanlışlıkla oluşturulmuş venv
path/to/venv/

# ─── ML / eğitim artefaktları ─────────────────────────────
catboost_info/
# eğitilmiş modeller (yeniden üretilebilir; istenirse tek tek: git add -f <dosya>)
*.pkl
mlruns/
optuna_*.db

# ─── Scraping debug çıktıları ─────────────────────────────
debug_html.html
page_source.html
foreign_html.html
atyarisicom_sonuclar.html
screenshot.png
*.log

# ─── Canlı döngü geçici çıktıları (her çalıştırmada yeniden üretilir) ─
program_tablo.csv
master_live.csv
predictions_*.md
runs/
# Faz 3 — bahis stratejisi (yeniden üretilebilir / scraping ile dolar)
oof_predictions.csv
betting_strategy_backtest.csv
bets_*.md
payouts_tablo.csv
# Not: predictions_log.csv ve live_performance.csv forward-test KAYDIDIR
#      (birikimli kanıt) → bilinçli olarak izlenir, ignore edilmez.

# ─── Büyük veri dosyaları (scraper ile yeniden üretilebilir) ─
# Tezde paylaşmak istersen bu satırları kaldır veya: git add -f <dosya>
yaris_ana_tablo.csv
atlar_statik_tablo.csv
idmanlar_tablo.csv
master_feature_matrix.csv

# ─── İşletim sistemi ──────────────────────────────────────
.DS_Store
Thumbs.db

# ─── IDE ──────────────────────────────────────────────────
.idea/
.vscode/
*.swp

# NOT (bilinçli olarak izlenenler):
#   reports/  → sonuç tablo & grafikleri (küçük, repoda kalsın)
#   models/*.json → metadata + production_registry.json (küçük, kalsın)
#   RAPOR.md, README.md → dokümantasyon
